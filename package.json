{
  "name": "cartback",
  "version": "0.1.0",
  "description": "Minimal Shopify abandoned checkout recovery via Vapi voice agent",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
